# --- AI Provider Configuration ---
# Options: "openai", "xai", "gemini", "custom", "auto"
# "auto" will try to detect keys from environment or system keyring.
AI_PROVIDER="auto"

# --- API Keys & Endpoints ---
# Required if AI_PROVIDER is "openai" or "auto".
# NOTE: Even if using Grok (xAI) or Gemini via their OpenAI-compatible endpoints, 
# you should set this variable to your provider's API key.
OPENAI_API_KEY="sk-..."

# Optional: Custom Base URL (e.g. for Grok or Local LLMs)
# OPENAI_BASE_URL="https://api.openai.com/v1"

# Basic Authentication (Optional but recommended for deployment)
# AUTH_USERNAME=admin
# AUTH_PASSWORD=secret"

# Optional: World Data Directory
# Defaults to "worlds" (relative to project root).
# If you change this to an absolute path locally, remember to override it 
# when running in Docker (e.g. -e WORLD_DATA_DIR=/app/worlds).
# WORLD_DATA_DIR=worlds

# --- Model Configuration ---
# Overrides the provider defaults if set
# LLM_MODEL="gpt-4-turbo"
# IMAGE_GEN_MODEL="dall-e-3"

# --- Image Generation Specifics ---
# Only needed if using a different provider for images
# IMAGE_GEN_API_KEY="sk-..."
# IMAGE_GEN_BASE_URL="https://api.openai.com/v1"

# --- System Prompts (Optional Overrides) ---
# NOTE: These are global defaults. When creating a new world, the "Magic Config" 
# or manual settings will generate world-specific prompts that overwrite these.
# SYSTEM_PROMPT_PLANNER="You are a creative world-building assistant..."
# SYSTEM_PROMPT_WRITER="You are an encyclopedic writer..."
# SYSTEM_PROMPT_IMAGE="You are an expert art director..."
